<section class="skills module">
	<h1>Skills <small>(click skills for more details)</small></h1>
	<article>
		<div class="large">
			<ul>
				{{#each skill in controller}}
					{{#view view.SkillItem}}
						<span>{{skill.title}}</span>
							<p class="desc">{{skill.description}}</p>
					{{/view}}
				{{/each}}
			</ul>
		</div>
	</article>
	{{outlet}}
</section>